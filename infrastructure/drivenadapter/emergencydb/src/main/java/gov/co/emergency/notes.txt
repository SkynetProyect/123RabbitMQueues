Yeah usually a DB should have many more entities and maintain a normalization till 3rd formal mode, but, this is just an example right?.

implementation 'org.postgresql:r2dbc-postgresql:0.8.1.RELEASE'